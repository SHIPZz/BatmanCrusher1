using UnityEngine;

public class OnPlayingButtonClicked : MonoBehaviour
{
    //public bool IsClicked { get; private set; } = false;

    //private void OnEnable()
    //=> PlayerInput.MouseDown += OnClicked;

    //private void OnDisable()
    //=> PlayerInput.MouseDown -= OnClicked;

    //private void OnClicked() =>
    //    IsClicked = true;
}